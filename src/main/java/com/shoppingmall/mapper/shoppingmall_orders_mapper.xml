<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="mapper.orders">
	<select id="info" resultType="com.shoppingmall.vo.Myshop_ordersVO">
		select oid, id, pname, brand, price, psfile, category_id, myshop_product.pid
			from myshop_orders, myshop_product
			where myshop_orders.pid = myshop_product.pid
	</select>
</mapper>